{
  "name": "validate-semver-release",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage && standard"
  },
  "dependencies": {
    "@actions/core": "^1.1.1",
    "actions-toolkit": "^2.2.0",
    "semver": "^6.3.0"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "standard": "^14.3.1"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
